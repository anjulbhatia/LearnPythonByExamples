# This program checks whether a user is eligible to vote or not.
# We will use if-else statements and a relational operator (>=) here.

age = int(input("Enter Age: "))

if age >= 18:
    print("Eligible to vote.")
else:
    print("Not eligible to vote.")